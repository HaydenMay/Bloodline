/**
 * render/ — canvas drawing, the skeletal horse rig, sprite layering, camera.
 *
 * May import from sim/ (to read state) but sim/ must never import from here.
 * Lands in Phase 2.
 */

export {};
